// packages/phantom-core/src/utils/responsive.ts
// TODO: Do we need to remove this? What is the purpose of this?

const responsive = (styles: Record<string, any>) => {
  console.warn('Placeholder responsive util called with:', styles);
  return styles;
};

export default responsive;