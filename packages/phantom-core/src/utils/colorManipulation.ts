// packages/phantom-core/src/utils/colorManipulation.ts
// TODO: Do we need to remove this? What is the purpose of this?

const colorManipulation = {
  lighten: (color: string, amount: number): string => {
    console.warn(`Placeholder: Lighten ${color} by ${amount}`);
    return color;
  },
  darken: (color: string, amount: number): string => {
    console.warn(`Placeholder: Darken ${color} by ${amount}`);
    return color;
  },
};

export default colorManipulation;