# packages/phantom-core/.npmrc
# Configuration for npm

# Example: Save exact versions of dependencies
save-exact=true

# Example: Define package hoisting limits for monorepos (if applicable)
# hoist-pattern[]=*

# Example: Use legacy peer dependency resolution if needed
# legacy-peer-deps=true